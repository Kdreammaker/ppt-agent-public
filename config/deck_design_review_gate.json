{
  "schema_version": "1.0",
  "mode": "enforce_thresholds",
  "thresholds": {
    "max_issues": 38,
    "max_warnings": 2,
    "max_info": 36
  },
  "warning_codes_observed": [
    "layout_monotony"
  ],
  "notes": [
    "This gate blocks regressions beyond the current curated-template baseline.",
    "B20 lowered thresholds after shortening regression sample copy to remove generic titles, near-budget values, and excessive deterministic cutoff events."
  ]
}
